const initialState = {
    textArea: '',
    error: {
        error: { error: false, message: '' }
    }
}

function reducer(state, action) {
    switch(action.type) {
        case '':
            return {
                ...state
            }
    }
}